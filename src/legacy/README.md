# Superseded material

`make_summary_figures.py` generated the earlier figure set from the RC1 summary
CSVs in `data/legacy_rc1/`. It is kept because those figures are cited as
Supplementary Figures S1-S5, but it is not part of the verified pipeline and its
outputs are not checked against the frozen tables. New work should use
`src/cazavi/`.
